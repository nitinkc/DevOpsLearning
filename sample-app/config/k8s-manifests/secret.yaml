apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: production
  labels:
    app: api-server
type: Opaque
data:
  # Base64 encoded values — replace with your own
  # echo -n "yourpassword" | base64
  DB_PASSWORD: Y2hhbmdlbWU=       # changeme
  API_KEY: c2VjcmV0LWtleS0xMjM=  # secret-key-123

